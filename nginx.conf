server {
    root  /docroot;
    charset  utf-8;

    location / {
        try_files  $uri  @app;
    }

    location @app {
        proxy_pass  http://rademade-admin-demo_app:3000;
        proxy_set_header  Host  $http_host;
        proxy_set_header  X-Real-IP  $remote_addr;
        proxy_set_header  X-Forwarded-For  $proxy_add_x_forwarded_for;
    }
}
