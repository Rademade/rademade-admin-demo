#!/bin/sh

bundle check || bundle install
yarn check || yarn install

rm -rf public/packs/* && rm -rf tmp/pids/*

wait-for-it -t 30 -h ${MYSQL_HOST} -p 3306
wait-for-it -t 30 -h ${MONGO_HOST} -p 27017

bundle exec rails server --port 3000 --binding 0.0.0.0
